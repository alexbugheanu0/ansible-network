---
- name: Manage VLANs on Cisco switches
  hosts: cisco_switches
  gather_facts: false
  vars:
    vlan_definitions: []
  tasks:
    - name: Ensure VLANs exist
      cisco.ios.ios_vlans:
        config: "{{ vlan_definitions }}"
        state: merged
      when: vlan_definitions | length > 0
